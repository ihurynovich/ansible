---
# The playbook deploys java application.

- hosts: tomcat_mysql
  user: vagrant
  sudo: yes

  roles:
    - tomcat
    - mysql
    - apache
    - deploy

  handlers:
    - include: handlers/handlers.yml
