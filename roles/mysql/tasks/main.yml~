---
  - name: install mysql 
    yum: name={{item}}
         state=present
    with_items:
      - mysql-server
      - mysql
      - MySQL-python

  - name: Start mysql
    service: name=mysqld state=running
